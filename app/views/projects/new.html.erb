<%= form_for @project do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label :description %>
  <%= f.text_field :description %><br>
  <%= f.collection_select(:lead_id, User.all, :id, :name) %><br>
  <%= f.submit %>
<% end %>